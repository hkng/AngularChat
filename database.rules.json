{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  "rules": {
    "messages": {
      ".read": "auth != null", // Allow only authenticated
      ".write": "auth != null", // Allow only authenticated
    },
    "user" : {
      ".read" : "auth !=null",
      ".write" : "auth !=null"
    }
  }
}
